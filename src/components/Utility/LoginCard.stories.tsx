import React from "react";
import LoginCard from "./LoginCard";

// eslint-disable-next-line import/no-anonymous-default-export
export default {
  title: "Components/LoginCard",
  component: LoginCard,
};

const Template = () => <LoginCard />;

export const Default = Template.bind({});
